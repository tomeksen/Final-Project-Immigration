'use client'

type Category = {
    id: string;
    application_id: string;
    name: string;
    order: number;
    status: string;
  };
  
export function CategoryManagerForm() {
  return (
    <div>

    </div>
  );
}